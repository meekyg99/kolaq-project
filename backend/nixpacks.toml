[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["rm -f tsconfig.tsbuildinfo dist/tsconfig.tsbuildinfo 2>/dev/null || true", "npm run build"]

[start]
cmd = "npm start"
